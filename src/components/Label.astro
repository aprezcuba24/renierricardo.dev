---
interface Props {
  label: string;
  class?: string;
}
const { label, class: classProps } = Astro.props;
---

<strong class:list={["mb-1 block font-semibold text-zinc-100", classProps]}
  >{label}</strong
>
